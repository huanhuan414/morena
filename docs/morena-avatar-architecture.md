# 莫瑞娜 · 分身产品架构

## 一、核心主线

```
像你 → 有技能 → 能变现
 ↑        ↑         ↑
 学习    提炼      部署
```

**一句话**：分身越来越像你 → 你的经验变成可复用的技能 → 技能进入你的工作/生活场景解决问题，或者租给别人赚钱。

---

## 二、第一层：像你（身份层）

### 2.1 "像你"是什么

不是泛泛的"性格相似"，而是四个维度全部对齐：

| 维度 | 定义 | 具体表现 | 学习来源 |
|------|------|----------|----------|
| **说话方式** | 你怎么表达 | 用词偏好、句式结构、语气节奏、口头禅、表情符号习惯 | 聊天记录导入、对话教学、纠正学习 |
| **决策方式** | 你怎么判断 | 面对问题的思考路径、优先级取舍、风险偏好 | 情景对话、决策案例、纠正学习 |
| **知识体系** | 你知道什么 | 专业知识、行业经验、踩坑记录、人脉资源 | 文档上传、聊天记录、网页链接、对话教学 |
| **底线规则** | 你绝不做什么 | 不报价/不承诺/不评价某人/涉及X必须转交本人 | 手动设置、纠正中的红线触发 |

### 2.2 学习方式

| 方式 | 速度 | 深度 | 适合学什么 | 用户门槛 |
|------|------|------|-----------|---------|
| **喂资料** | 快（30分钟） | 中 | 知识层：专业知识、产品信息、业务流程 | 低，上传即可 |
| **导入聊天记录** | 快（1小时） | 高 | 风格层+知识层：说话习惯+日常决策 | 低，导出微信聊天即可 |
| **对话教学** | 中（5轮对话） | 高 | 风格层+判断层：说话方式+决策逻辑 | 中，需要用户示范 |
| **纠正学习** | 慢（持续） | 最高 | 全层：真实场景下的实时修正 | 低，看到不对就纠正 |
| **手动设置** | 即时 | 精确 | 底线层：红线规则 | 低，勾选+描述 |

### 2.3 学习闭环

```
输入（资料/对话/聊天记录）
  ↓
AI解析提取
  ↓
生成学习结果 → 用户确认/修正
  ↓
写入四层人格
  ↓
分身下次回复时体现
  ↓
用户继续纠正 → 持续进化
```

**关键设计**：每条学习结果必须让用户看到，用户可以确认/删除/修改。分身不能偷偷改自己。

---

## 三、第二层：有技能（能力层）

### 3.1 技能是什么

**技能 = 一组可复用的决策+行动模式，能在特定场景下产生可预期的结果**

不是"我会Python"，而是：
- "客户嫌贵时，我会拆解单价对比→强调品质差异→提供试用方案" → **价格异议化解术**
- "收到模糊需求时，我会追问3层：业务目标→使用场景→验收标准" → **需求澄清术**
- "项目延期时，我会先梳理阻塞点→按影响排序→给出2个以上方案" → **危机应对术**

### 3.2 技能从哪来

技能不是用户手动创建的，是从"像你"的过程中**自动提炼**出来的：

```
分身在值守/对话中执行了多次相似操作
  ↓
系统识别到模式（你在X场景下总是Y做法）
  ↓
生成技能草稿 → 用户确认
  ↓
技能上架（可自用，可出租）
```

**提炼触发条件**：
- 同类场景出现3次以上 + 用户未纠正 → 系统认为这是你的固定模式
- 用户纠正3次以上都是同一方向 → 系统认为这是你的明确偏好
- 用户主动说"我遇到XX时一般这样处理" → 直接提取

### 3.3 技能的构成

```
一个技能 = 
  场景描述（什么时候用）
  + 触发条件（什么信号激活）
  + 决策链（if-then规则，3-7步）
  + 风格要求（用谁的说话方式回复）
  + 底线约束（绝对不能做什么）
  + 来源证据（从哪些对话/纠正中提炼的）
```

**示例**：
```
技能名：价格异议化解术
场景：客户/用户认为价格过高时
触发：消息中包含"贵""太贵""便宜""优惠""别人家"
决策链：
  1. 先共情（理解您的比较/确实不便宜）
  2. 拆解单价（算到每次/每天/每件的成本）
  3. 强调差异（和低价选项的品质/服务差异）
  4. 降低门槛（试用/小份量/首单优惠）
风格：用小林分身的说话方式
底线：不主动降价超过10%，不贬低竞品
来源：鲜果优选客服对话893条，被纠正23次，提炼于2024-12
```

### 3.4 技能的分类

| 类型 | 说明 | 示例 | 变现潜力 |
|------|------|------|---------|
| **沟通技能** | 对话中的策略和技巧 | 需求澄清、投诉处理、价格谈判 | 高（所有行业都需要） |
| **决策技能** | 分析判断的模式 | 方案评审、风险评估、优先级排序 | 中（偏专业领域） |
| **执行技能** | 完成具体任务的方法 | 快速排期、文档审查、数据整理 | 中（可被模板替代） |
| **创意技能** | 创造性输出的风格 | 文案撰写、方案设计、头脑风暴 | 高（风格独特性强） |
| **领域技能** | 行业专业能力 | 法律合规审查、医疗分诊、财务分析 | 最高（门槛高，竞争少） |

---

## 四、第三层：能变现（价值层）

### 4.1 变现的三条路径

```
路径A：分身替你工作（省时间）
  你自己的飞书群/企微群/小程序客服
  → 分身替你回复 → 你省时间
  → 你纠正它 → 它越来越像你
  → 价值 = 你省下的时间 × 你的时薪

路径B：技能出租（赚被动收入）
  你分身提炼出的技能 → 上架技能市场
  → 别人的分身/小程序租用你的技能
  → 价值 = 技能单价 × 使用次数

路径C：分身托管（赚月租）
  你的整个分身 → 租给商家当客服
  → 商家上传资料+纠正 → 分身学会商家风格
  → 价值 = 月租费用
```

### 4.2 场景部署

| 场景 | 接入方式 | 分身做什么 | 学习来源 |
|------|---------|-----------|---------|
| **飞书群聊** | WebSocket长连接 | 监听群消息，@分身时回复 | 群聊纠正、观察你回复 |
| **企业微信群** | 回调URL（需备案域名） | 监听群消息，@分身时回复 | 群聊纠正、观察你回复 |
| **小程序客服** | 微信客服消息推送 | 用户联系客服时分身自动回复 | 客服纠正、对话记录 |
| **别的小程序客服** | 托管回调/API | 去别人的小程序当客服 | 商家纠正、上传资料 |
| **和分身直接对话** | App内聊天 | 陪练、教学、测试 | 对话教学、纠正 |

### 4.3 定价模型

| 模式 | 定价 | 适合谁 | 分成 |
|------|------|--------|------|
| 按次付费 | ¥1-5/次 | 偶尔用技能的用户 | 平台20% + 用户80% |
| 按月订阅 | ¥99-299/月 | 长期使用技能的商家 | 平台20% + 用户80% |
| 分身托管 | ¥299-999/月 | 需要全职客服的商家 | 平台30% + 用户70% |
| 按效果付费 | 按成交/转化分成 | 电商类客户 | 平台30% + 用户70% |

---

## 五、产品架构图

```
┌─────────────────────────────────────────────────────┐
│                    莫瑞娜 App                        │
├──────────┬──────────┬──────────┬──────────┬─────────┤
│  分身首页  │  学习中心  │  技能工坊  │  工作台   │  收益   │
│          │          │          │          │         │
│ ·分身卡片 │ ·喂资料   │ ·提炼看板 │ ·飞书群   │ ·总览   │
│ ·相似度   │ ·导入聊天 │ ·我的技能 │ ·企微群   │ ·按来源 │
│ ·今日学习 │ ·对话教学 │ ·技能详情 │ ·小程序   │ ·提现   │
│ ·快捷入口 │ ·纠正记录 │ ·上架定价 │ ·添加场景 │         │
│          │ ·人格画像 │ ·技能市场 │ ·实时监控 │         │
└──────────┴──────────┴──────────┴──────────┴─────────┘
         │              │              │
         ▼              ▼              ▼
┌─────────────────────────────────────────────────────┐
│                   分身引擎 (Backend)                  │
├──────────┬──────────┬──────────┬──────────┬─────────┤
│ 人格系统  │  学习系统  │  技能系统  │  部署系统  │ 变现系统 │
│          │          │          │          │         │
│ ·四层架构 │ ·文档解析 │ ·模式识别 │ ·飞书连接 │ ·技能市场│
│ ·底线层   │ ·聊天提取 │ ·技能提炼 │ ·企微连接 │ ·计费   │
│ ·判断层   │ ·对话学习 │ ·技能验证 │ ·客服回调 │ ·分成   │
│ ·风格层   │ ·纠正学习 │ ·技能组合 │ ·API网关  │ ·结算   │
│ ·知识层   │ ·偏好沉淀 │ ·技能版本 │ ·消息路由 │         │
└──────────┴──────────┴──────────┴──────────┴─────────┘
         │              │              │
         ▼              ▼              ▼
┌─────────────────────────────────────────────────────┐
│                    LLM + 存储                        │
├─────────────────────────────────────────────────────┤
│ ·豆包大模型（分身对话、技能提炼、学习解析）            │
│ ·向量数据库（知识层RAG、技能检索）                    │
│ ·MySQL（用户数据、分身数据、订单数据）                │
│ ·Redis（缓存、会话状态、计数器）                      │
└─────────────────────────────────────────────────────┘
```

---

## 六、产品页面设计

### 6.1 页面清单

| 页面 | 核心任务 | 用户价值 | 关键指标 |
|------|---------|---------|---------|
| **分身首页** | 一眼看到分身状态 | 知道分身像不像我、在不在干活 | 相似度、今日学习数、活跃场景数 |
| **学习中心** | 让分身像我的操作入口 | 5分钟就能让分身多学一点 | 学习方式使用率、学习条数、确认率 |
| **人格画像** | 看分身学到了什么 | 透明可控，不是黑盒 | 四层条目数、用户编辑率 |
| **技能工坊** | 技能提炼和管理 | 我的经验变成了可变现的资产 | 技能数、提炼成功率、上架率 |
| **工作台** | 部署分身到工作场景 | 分身真正在替我干活 | 活跃场景数、回复数、响应时间 |
| **收益中心** | 看赚了多少钱 | 变现闭环 | 总收入、月增长、提现金额 |

### 6.2 页面流转

```
分身首页
  ├── 点"喂资料" → 学习中心（资料上传）
  ├── 点"对话学" → 学习中心（对话教学）
  ├── 点"人格"   → 人格画像（四层详情）
  ├── 点"技能"   → 技能工坊（技能列表）
  ├── 点"工作台" → 工作台（场景部署）
  └── 点"收益"   → 收益中心

学习中心
  ├── 上传资料 → AI解析 → 学习结果确认 → 写入人格
  ├── 导入聊天 → AI提取习惯/知识 → 确认 → 写入人格
  ├── 对话教学 → 场景模拟 → 你教它学 → 写入人格
  └── 纠正记录 → 查看历史 → 可修改/删除

技能工坊
  ├── 提炼看板 → 系统发现的新模式 → 确认/忽略
  ├── 我的技能 → 技能列表 → 详情/编辑/上架
  ├── 技能详情 → 构成/来源证据/使用记录
  └── 技能市场 → 浏览/租用别人的技能

工作台
  ├── 飞书群聊 → 添加群 → 开启值守 → 实时监控
  ├── 企微群聊 → 添加群 → 开启值守 → 实时监控
  ├── 小程序客服 → 配置回调 → 开启值守 → 实时监控
  └── 别的小程序 → 生成接入码 → 对方配置 → 开始服务
```

### 6.3 每个页面的设计原则

**分身首页**：
- 相似度是最醒目的数字，用户第一眼就知道分身像不像我
- 今日学习条数让用户知道分身在持续进化
- 快捷操作不超过3个：喂资料、对话学、部署工作

**学习中心**：
- 学习方式按"快→慢"排序：喂资料（最快）→ 导入聊天 → 对话教学 → 纠正学习（最慢但最深）
- 每次学习结束必须展示"分身学到了什么"，让用户确认
- 学习结果标注写入哪层，用户可修改/删除

**人格画像**：
- 四层架构可视化，每层可展开看具体条目
- 每条标注来源（手动设置/聊天纠正/资料导入/...）
- 置信度标签（高/中/低），低置信度的提示用户确认
- 相似度总分 = 四层加权得分

**技能工坊**：
- 提炼看板用"发现新技能"的方式呈现，像通知一样
- 每个技能必须有来源证据链（从哪几条对话/纠正中提炼的）
- 技能上架前必须用户确认，系统不能自动上架

**工作台**：
- 每个场景有实时状态（在线/离线/学习中）
- 关键指标实时更新：今日回复数、响应时间、纠正次数
- 一键开关值守

**收益中心**：
- 总收入 + 各来源分项
- 技能出租 vs 分身托管 vs 小程序客服 的收入对比
- 提现功能

---

## 七、技术架构

### 7.1 人格系统

```
分身人格 = 底线层 + 判断层 + 风格层 + 知识层

底线层（Hard Rules）：
  存储：JSON规则表，格式 { trigger, action, priority }
  示例：{ trigger: "涉及报价", action: "转交本人确认", priority: 1 }
  特点：不可被AI修改，只能用户手动增删

判断层（Case-Based Reasoning）：
  存储：向量数据库 + 结构化案例表
  示例：{ situation: "客户催进度", strategy: "先安抚+给时间节点+说明当前进展", confidence: 0.85 }
  特点：AI可建议新增，用户确认后写入

风格层（Style Injection）：
  存储：风格特征表 + 示例文本
  示例：{ feature: "分点式表达", evidence: "73%的回复使用分点结构", examples: [...] }
  特点：从聊天记录和对话中自动提取，持续更新

知识层（RAG）：
  存储：向量数据库（文档chunk）+ 结构化知识表
  示例：{ domain: "SaaS产品", fact: "基础版298/月含500次调用", source: "产品手册v3.2.pdf" }
  特点：文档导入自动解析，标注来源
```

### 7.2 学习系统

```
学习管线：
  输入 → 预处理 → AI提取 → 生成学习结果 → 用户确认 → 写入人格

文档解析：
  PDF/Word → 文本提取 → 按段落分chunk → 向量化存入知识层
  Excel → 按sheet解析 → 结构化数据存入知识层

聊天记录解析：
  微信导出txt → 按消息拆分 → 识别用户发言 → 
    → 提取说话习惯（风格层）
    → 提取决策模式（判断层）
    → 提取知识信息（知识层）

对话教学：
  用户给场景 → 用户示范回复 → AI分析示范 vs 通用回复的差异 →
    → 差异写入对应层

纠正学习：
  分身原版回复 vs 用户纠正后 → AI分析差异 →
    → 说话差异 → 风格层
    → 策略差异 → 判断层
    → 事实差异 → 知识层
    → 红线触发 → 底线层
```

### 7.3 技能系统

```
技能提炼：
  触发：同类场景3次+ 或 同方向纠正3次+
  流程：
    1. 聚类：从对话记录中找到相似的决策模式
    2. 抽象：提取 if-then 规则链
    3. 生成草稿：补全场景描述、触发条件、底线约束
    4. 用户确认：展示来源证据，用户确认/修改
    5. 上架：标记为可复用，设定定价

技能执行：
  输入消息 → 匹配技能（触发条件命中）→ 
    → 按决策链逐步执行 → 
    → 用指定风格层的说话方式生成回复 →
    → 检查底线约束 → 输出回复

技能组合：
  一条消息可能匹配多个技能 → 按优先级排序 → 取最高优先级执行
  或：多个技能的结果合并（如沟通技能+领域技能）
```

### 7.4 部署系统

```
消息路由：
  外部消息进入 → 识别来源（飞书/企微/小程序）→ 
    → 查找该场景绑定的分身 →
    → 分身处理（人格+技能）→ 
    → 生成回复 → 通过对应渠道发回

渠道适配：
  飞书：WebSocket长连接 → 监听im.message.receive_v1 → 调API回复
  企微：HTTP回调 → 验签+解密 → 调API回复
  小程序客服：HTTP回调 → 验签 → 调客服消息API回复
  别的小程序：托管回调/API → 同上
```

---

## 八、数据指标体系

### 8.1 分身健康度

| 指标 | 定义 | 目标 |
|------|------|------|
| 相似度 | 分身回复与用户本人回复的语义一致性 | >80% |
| 学习速率 | 每日新增学习条数 | >3条/天 |
| 纠正率 | 分身回复被纠正的比例 | <20% |
| 确认率 | 学习结果被用户确认的比例 | >70% |

### 8.2 技能健康度

| 指标 | 定义 | 目标 |
|------|------|------|
| 提炼成功率 | 系统提议的技能被用户确认的比例 | >60% |
| 技能使用率 | 上架技能被实际调用的次数 | >10次/月 |
| 技能满意度 | 技能执行后未被纠正的比例 | >85% |
| 技能复用率 | 一个技能被多少个不同场景使用 | >2 |

### 8.3 变现健康度

| 指标 | 定义 | 目标 |
|------|------|------|
| ARPU | 每个分身的月均收入 | >¥50 |
| 付费转化率 | 创建分身的用户中产生收入的比例 | >10% |
| 场景覆盖率 | 有收入的分身占总分身的比例 | >15% |
| 续费率 | 月租用户的续费比例 | >70% |

---

## 九、优先级排序

### P0（必须先做）
1. 学习中心（喂资料 + 对话教学）— 这是"像你"的入口
2. 人格画像 — 让用户看到分身学到了什么，建立信任
3. 纠正学习 — 所有部署场景都需要

### P1（紧接着做）
4. 工作台（飞书群聊值守）— 第一个真实场景
5. 技能工坊（提炼+管理）— "有技能"的闭环
6. 小程序客服接入 — 最大的C端场景

### P2（后续迭代）
7. 技能市场 — 变现闭环
8. 别的小程序接入 — 分身替别人当客服
9. 收益中心 — 完整变现链路

---

## 十、与现有产品的关系

### 10.1 现有功能保留

| 现有功能 | 在新架构中的位置 | 变化 |
|---------|----------------|------|
| 分身广场 | 分身首页的Tab | 不变 |
| 创建分身 | 分身首页 | 增加初始学习步骤 |
| 心聊对话 | 学习中心-对话教学 | 升级为教学+纠正模式 |
| 订单广场 | 技能市场 | 合并到技能市场 |
| 飞书群聊值守 | 工作台 | 不变，新增实时监控 |

### 10.2 新增功能

| 新功能 | 页面 | 优先级 |
|--------|------|--------|
| 喂资料 | 学习中心 | P0 |
| 导入聊天记录 | 学习中心 | P0 |
| 人格画像 | 独立页面 | P0 |
| 纠正学习 | 学习中心 | P0 |
| 技能提炼 | 技能工坊 | P1 |
| 小程序客服接入 | 工作台 | P1 |
| 技能市场 | 独立页面 | P2 |
| 别的小程序接入 | 工作台 | P2 |
| 收益中心 | 独立页面 | P2 |
