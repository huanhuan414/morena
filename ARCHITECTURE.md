# 分身大师 (Morena AI) — 产品架构与逻辑图

---

## 一、产品定位

**分身大师**是一个 AI 分身内容创作与分发平台。用户创建数字分身，由 AI 自动生成图文/视频内容并发布到各大社交平台，实现"省心副业"。

**核心价值**：一次设置 → AI 持续生成内容 → 自动/半自动发布 → 持续收益

---

## 二、系统架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        客户端 (Taro 跨端)                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────────┐ │
│  │  微信小程序 │  │  抖音小程序  │  │   H5 端    │  │  管理后台(H5)     │ │
│  └─────┬────┘  └─────┬────┘  └─────┬────┘  └───────┬──────────┘ │
│        └──────────────┴──────────────┴───────────────┘            │
│                           │                                       │
│                    Network.request / uploadFile                   │
└───────────────────────────┼──────────────────────────────────────┘
                            │
                     Vite Proxy / CDN
                            │
┌───────────────────────────┼──────────────────────────────────────┐
│                    NestJS 后端 (API 层)                            │
│  ┌─────────────────────────┼──────────────────────────────────┐  │
│  │                    中间件 / 守卫                              │  │
│  │     AuthGuard · RateLimit · Logger · Validation             │  │
│  └─────────────────────────┼──────────────────────────────────┘  │
│                           │                                       │
│  ┌─────────────┐  ┌──────┴──────┐  ┌──────────────┐             │
│  │  订单模块     │  │  分身模块    │  │  内容生成模块   │             │
│  │  OrderModule │  │ AvatarModule│  │ ContentGenMod│             │
│  └──────┬──────┘  └──────┬──────┘  └──────┬───────┘             │
│         │                │                 │                      │
│  ┌──────┴──────┐  ┌──────┴──────┐  ┌──────┴───────┐             │
│  │ 接单调度模块  │  │ 技能模块     │  │ 图片生成模块   │             │
│  │ OrderDispatch│  │ SkillModule │  │ ImageGenMod  │             │
│  └──────┬──────┘  └──────┬──────┘  └──────┬───────┘             │
│         │                │                 │                      │
│  ┌──────┴──────┐  ┌──────┴──────┐  ┌──────┴───────┐             │
│  │ 上传/存储模块 │  │ 支付模块     │  │ AI技能模块    │             │
│  │ UploadModule │  │ PayModule   │  │ AiSkillModule│             │
│  └──────┬──────┘  └──────┬──────┘  └──────┬───────┘             │
│         │                │                 │                      │
│  ┌──────┴──────┐  ┌──────┴──────┐  ┌──────┴───────┐             │
│  │ 用户模块     │  │ 通知模块     │  │ 推荐模块      │             │
│  │ UserModule  │  │ NotifModule │  │ RecommendMod │             │
│  └──────┬──────┘  └──────┬──────┘  └──────┬───────┘             │
│         └────────────────┴────────────────┘                      │
│                          │                                        │
│  ┌───────────────────────┴───────────────────────────────────┐   │
│  │                    数据访问层 (MySQL Client)                 │   │
│  │          db.query / db.insert / db.update / db.delete       │   │
│  └───────────────────────────┬───────────────────────────────┘   │
└──────────────────────────────┼───────────────────────────────────┘
                               │
          ┌────────────────────┼────────────────────┐
          │                    │                     │
   ┌──────┴──────┐    ┌───────┴───────┐    ┌───────┴───────┐
   │   MySQL     │    │    Redis      │    │  外部服务       │
   │  数据持久化   │    │  缓存/计数器   │    │               │
   │  285 用户    │    │  接单名额控制   │    │ · 火山引擎TOS  │
   │  628 分身    │    │  ZIP进度追踪   │    │ · veImageX    │
   │  67 订单     │    │  会话缓存      │    │ · GPT-Image-2 │
   │  40 付费订单  │    │               │    │ · DeepSeek LLM│
   └─────────────┘    └───────────────┘    │ · 微信支付      │
                                          │ · 公众号API     │
                                          │ · 抖音开放平台   │
                                          └───────────────┘
```

---

## 三、数据库 ER 图

```
┌──────────────┐     1:N     ┌──────────────────┐     1:N     ┌───────────────────┐
│    users      │────────────│     avatars       │────────────│  avatar_accounts   │
│──────────────│            │──────────────────│            │───────────────────│
│ id (PK)      │            │ id (PK)          │            │ id (PK)           │
│ openid       │            │ user_id (FK)     │            │ avatar_id (FK,✓NU)│
│ unionid      │            │ avatar_name      │            │ user_id           │
│ nickname     │            │ avatar_style     │            │ platform          │
│ avatar_url   │            │ personality      │            │ account_name      │
│ phone        │            │ platform         │            │ app_id / app_key  │
│ balance      │            │ status           │            │ access_token      │
│ created_at   │            │ avatar_image_url │            │ status            │
└──────┬───────┘            └────────┬─────────┘            └───────────────────┘
       │                             │
       │ 1:N                         │ N:M (通过 order_dispatch_requests)
       │                             │
┌──────┴───────┐            ┌────────┴──────────┐     1:N     ┌──────────────────┐
│    orders     │────────────│order_dispatch_reqs │────────────│  order_assets    │
│──────────────│            │──────────────────│            │──────────────────│
│ id (PK)      │            │ id (PK)          │            │ id (PK)          │
│ user_id (FK) │            │ order_id (FK)    │            │ order_id (FK)    │
│ title        │            │ avatar_id (FK)   │            │ asset_type       │
│ description  │            │ status           │            │ source           │
│ status       │            │ content_result   │            │ file_url         │
│ is_paid      │            │ published_url    │            │ assigned_to      │
│ accept_limit │            │ published_at     │            │ sort_order       │
│ amount       │            │ accepted_at      │            │ status           │
│ platform     │            │                  │            │ prompt           │
│ mode(shared/ │            └──────────────────┘            └──────────────────┘
│   exclusive) │
│ avatar_count │            ┌──────────────────┐            ┌──────────────────┐
│ has_user_    │    1:N     │    payments       │    1:N     │   ai_skills      │
│   assets     │────────────│──────────────────│            │──────────────────│
│ created_at   │            │ id (PK)          │            │ id (PK)          │
└──────────────┘            │ order_id (FK)    │            │ name             │
                            │ amount           │            │ description      │
                            │ status           │            │ category         │
                            │ transaction_id   │            │ config           │
                            └──────────────────┘            │ status           │
                                                            └──────────────────┘
```

---

## 四、核心业务流程图

### 4.1 发单流程（订单创建 → 素材准备 → 开放接单）

```
用户点击「发单」
       │
       ▼
┌──────────────┐
│ 选择发布平台   │  微信公众号 / 抖音 / 小红书 / 快手 / B站
│ 填写标题/描述  │
│ 选择分身数量   │
│ 选择分发模式   │  共享模式(所有分身用同一套素材) / 独占模式(素材不重叠)
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ 上传素材      │  单张上传 / 批量上传 / ZIP压缩包上传
│ (图片+视频)   │  实时进度：解压中 → 图片1上传中 → 图片2上传中 → 完成
└──────┬───────┘
       │
       ▼
┌──────────────┐     ┌───────────────────────┐
│ AI 补齐素材?  │──否─▶│ 跳过，使用用户上传的素材  │
│ (素材不足时)  │     └───────────┬───────────┘
└──────┬───────┘                 │
       │ 是                      │
       ▼                         │
┌──────────────┐                 │
│ GPT-Image-2  │                 │
│ 自动生成图片  │                 │
│ + 生成视频    │                 │
└──────┬───────┘                 │
       │                         │
       ▼                         ▼
┌──────────────┐
│ 素材准备完成   │  状态: pending_acceptance / in_progress
│ 等待支付      │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│  微信支付     │
└──────┬───────┘
       │ 支付成功
       ▼
┌──────────────┐
│ 素材全部ready?│──否─▶│ 订单不在广场展示，等待AI补齐 │
└──────┬───────┘      └──────────┬──────────────┘
       │ 是                       │
       ▼                          │ (素材补齐后自动展示)
┌──────────────┐                  │
│ 订单进入广场   │◀────────────────┘
│ 开放接单      │
└──────────────┘
```

### 4.2 接单流程（分身匹配 → 内容生成 → 发布）

```
接单方浏览「订单广场」
       │
       ▼
┌──────────────┐
│ 匹配推荐算法   │  根据分身风格、平台、标签匹配最佳订单
│ 展示推荐列表   │
└──────┬───────┘
       │ 点击「去接单」
       ▼
┌──────────────────────────┐
│ 检查接单条件               │
│ · 订单状态 in acceptable   │
│ · 接单名额未满             │
│ · Redis 原子计数器控制     │
└──────┬───────┬───────────┘
      成功     失败
       │       │
       ▼       ▼
  接单成功   提示「名额已满/
       │      订单已关闭」
       ▼
┌──────────────┐
│ AI 自动生成内容│
│ · DeepSeek 生成文案
│ · 匹配素材到分身 │  共享: 所有人用同一套
│ · 生成发布建议   │  独占: 素材按分身不重叠分配
└──────┬───────┘
       │
       ▼
┌──────────────┐
│  内容预览/编辑  │
│  文案 + 图片 + 视频
└──────┬───────┘
       │
       ▼
┌──────────────┐     ┌──────────────┐
│  发布方式      │     │  发布反馈      │
│  · 自动发布    │────▶│  成功/失败     │
│  · 手动复制    │     │  发布链接记录   │
│  · 发布指导    │     └──────────────┘
└──────────────┘
```

### 4.3 订单状态流转图

```
                    ┌─────────────┐
                    │pending_payment│  刚创建，未支付
                    └──────┬──────┘
                           │ 支付成功
                           ▼
              ┌────────────────────────┐
              │                        │
     素材未准备完               素材已准备完
              │                        │
              ▼                        ▼
     ┌────────────────┐    ┌───────────────────┐
     │ pending_accept │    │ awaiting_acceptance│  等待接单(广场可见)
     │ (广场不可见)    │    │ (广场可见)         │
     └───────┬────────┘    └─────────┬─────────┘
             │ 素材准备完             │ 有人接单
             └────────────┬──────────┘
                          ▼
                ┌──────────────────┐
                │   in_progress     │  接单进行中
                └─────────┬────────┘
                          │ 所有分身完成
                          ▼
                ┌──────────────────┐
                │    submitted      │  内容已提交
                └─────────┬────────┘
                          │ 发布确认
                          ▼
                ┌──────────────────┐
                │    completed      │  订单完成
                └──────────────────┘
```

---

## 五、前端页面架构

```
TabBar (3个主Tab)
├── 首页 (pages/index)
│   ├── 订单广场模块 ──────▶ 订单详情/接单
│   ├── 分身推荐模块 ──────▶ 分身详情
│   └── 技能广场入口 ──────▶ 技能列表
│
├── 分身 (pages/mind-chat)
│   ├── 分身对话 (AI聊天)
│   ├── 分身列表/切换
│   └── 快捷操作 ──────▶ 创建分身/技能
│
└── 我的 (pages/profile)
    ├── 我的订单 ──────▶ 订单列表/详情
    ├── 我的分身 ──────▶ 分身管理
    ├── 收益中心
    ├── 设置
    └── 关于/帮助

分包页面
├── package-order (订单相关)
│   ├── order-square      订单广场(完整列表)
│   ├── order-create      创建订单
│   ├── order-detail      订单详情
│   ├── order-matching    订单匹配推荐
│   ├── order-acceptance  接单页面
│   ├── order-content-creation  内容创作
│   ├── order-asset-waiting     素材等待
│   ├── order-publish-guide     发布指导
│   ├── order-completed         订单完成
│   └── order-stats       订单统计
│
├── package-avatar (分身相关)
│   ├── avatar-manage     分身管理
│   ├── avatar-create     创建分身
│   ├── avatar-profile    分身详情
│   ├── avatar-settings   分身设置
│   ├── avatar-account-config  账号绑定(公众号等)
│   ├── oauth-webview     OAuth授权
│   └── social            社交互动
│
├── package-skill (技能相关)
│   ├── skills-square     技能广场
│   ├── skill-create      创建技能
│   ├── palm-reading      看手相
│   ├── fashion-makeover  衣品改造
│   └── wechat-mp-article 公众号爆款图文
│
├── package-profile (个人相关)
│   ├── settings          设置
│   ├── earning-center    收益中心
│   ├── referral-center   推荐中心
│   └── earnings-wall     收益墙
│
└── package-admin (管理后台)
    ├── dashboard         数据看板
    ├── users             用户管理
    ├── avatars           分身管理
    ├── orders            订单管理
    ├── finance           财务管理
    └── skills            技能管理
```

---

## 六、后端模块架构

```
server/src/modules/
├── agent/               AI Agent 模块 (分身对话、内容生成Agent)
│   └── avatar-agent/    分身Agent工具链
│       └── tools/       内容生成工具、平台适配工具
│
├── ai-skill/            AI 技能模块
│   └── 看手相/衣品改造/公众号图文 等技能处理
│
├── auth/                认证模块
│   └── 微信登录 / 手机号登录 / JWT
│
├── avatar/              分身模块
│   └── CRUD / 账号绑定 / 分身推荐
│
├── content-generation/  内容生成模块 (核心)
│   └── 文案生成 / 素材预生成 / 发布策略 / 质量自检
│
├── image-gen/           图片生成模块
│   └── GPT-Image-2 API 调用 / b64_json 解析 / 上传TOS
│
├── notification/        通知模块
│   └── 系统通知 / 订单状态变更通知
│
├── order/               订单模块
│   └── 订单CRUD / 状态流转 / 素材管理 / 订单广场
│
├── order-assets/        订单素材模块
│   └── 素材CRUD / 批量绑定 / AI补齐 / 素材分配
│
├── order-dispatch/      接单调度模块
│   └── 推荐算法 / 接单/拒单 / Redis名额控制
│
├── payment/             支付模块
│   └── 微信支付 / 支付回调 / 退款
│
├── upload/              上传模块
│   └── 单文件上传 / ZIP上传(实时进度) / TOS对象存储
│
├── user/                用户模块
│   └── 用户CRUD / 余额 / 统计
│
├── user-stats/          用户统计模块
│   └── 收益统计 / 接单统计 / 活跃度
│
├── redis/               Redis 模块 (全局)
│   └── 缓存 / 计数器 / ZIP进度 / 会话
│
├── storage/             对象存储模块
│   └── 火山引擎TOS / veImageX CDN
│
└── volcengine/          火山引擎模块
    └── TOS上传 / veImageX图片处理 / 视频上传
```

---

## 七、核心外部服务集成

```
┌───────────────────────────────────────────────────┐
│                   外部服务                          │
├───────────────┬───────────────────────────────────┤
│ 火山引擎 TOS   │ 文件存储：图片/视频/ZIP上传          │
│               │ CDN加速：voic.51webjs.com           │
├───────────────┼───────────────────────────────────┤
│ veImageX      │ 图片处理：缩略图/裁剪/水印            │
│               │ URL模板：~tplv-699z2ac540-image.png │
├───────────────┼───────────────────────────────────┤
│ GPT-Image-2   │ AI图片生成（b64_json → TOS → URL）   │
│ api.aaigc.top │ 支持 1024x1024 / 1024x1536         │
├───────────────┼───────────────────────────────────┤
│ DeepSeek      │ 大语言模型：文案生成/对话/技能推理      │
├───────────────┼───────────────────────────────────┤
│ 微信支付       │ 订单支付/回调/退款                    │
├───────────────┼───────────────────────────────────┤
│ 公众号API      │ 素材上传/草稿发布/自动发布             │
├───────────────┼───────────────────────────────────┤
│ 抖音开放平台   │ 视频发布/账号授权                     │
└───────────────┴───────────────────────────────────┘
```

---

## 八、关键数据流

### 8.1 素材上传与分配

```
用户上传素材
    │
    ├── 单张上传 ──▶ Multer → veImageX → URL → order_assets表
    │
    ├── ZIP上传 ──▶ Multer → 解压 → 逐个上传veImageX
    │               │              ↑
    │               │         Redis实时进度推送
    │               │         (解压中/图片1上传中...)
    │               ▼
    │          返回URL列表 → 批量写入order_assets
    │
    └── AI补齐 ──▶ GPT-Image-2生成 → b64解码 → TOS上传 → URL

素材分配：
    ├── 共享模式：所有分身获得相同的完整素材集
    └── 独占模式：素材不重叠，每分身上限 = ceil(总数/分身数)
                  用 assigned_to 字段原子占位
```

### 8.2 内容生成与发布

```
接单成功
    │
    ▼
DeepSeek LLM 生成文案
    │
    ├── 系统提示词：分身人设 + 平台规则 + 素材信息
    ├── 生成：标题 + 正文 + 标签 + 发布建议
    └── 质量自检：格式/字数/敏感词

匹配素材到文案
    │
    ▼
发布策略
    │
    ├── 自动发布（公众号）：API直接发布
    ├── 手动复制：一键复制文案 + 下载图片
    └── 发布指导：步骤说明 + 最佳发布时间
```

---

## 九、技术栈总览

| 层级 | 技术 |
|------|------|
| 前端框架 | Taro 4 + React + TypeScript |
| UI组件 | shadcn/ui (Taro版) + Tailwind CSS 4 |
| 小程序适配 | weapp-tailwindcss + lucide-react-taro |
| 后端框架 | NestJS + TypeScript |
| 数据库 | MySQL 8.0 (云RDS) |
| 缓存 | Redis (计数器/进度/会话) |
| 对象存储 | 火山引擎 TOS + veImageX CDN |
| AI模型 | GPT-Image-2 (图片) + DeepSeek (文本) |
| 支付 | 微信支付 |
| 部署 | 服务器 180.184.205.74 + PM2 + Nginx |
| 包管理 | pnpm (monorepo) |
