# 残余问题登记（P2 / 不阻断验收）

## 兼容层与历史字段
- 页面/业务仍可能存在对历史字段的读取残留，需要按“adapter 单点只读兼容 → 两轮回归后清零 → 删除映射”的策略持续清理。

## 邀请数据一致性
- 当前通过“锁 invitee 用户行”的事务化幂等降低并发重复插入风险；建议后续在数据库层补齐 `referrals(referred_id)` 唯一约束，彻底兜底并发竞态。

## 低频页面与引导素材
- 新手引导等低频页面仍存在静态素材占位图/资源合规问题，需按对象存储规范替换为真实 TOS URL。

